<h1> <%= @cocktail.name%> </h1>

<% @cocktail.doses.each do |dose| %>
  <p> <%= dose.description %> <%= dose.ingredient.name %> <%= link_to "Delete this dose", dose_path(dose),
      method: :delete,
      data: { confirm: "Are you sure?" } %></p>
<% end %>

<%=link_to "Back to home", root_path %>

<%= link_to "Add a dose", cocktail_doses_path(@cocktail) %>

<%= link_to "Delete this cocktail", cocktail_path(@cocktail),
    method: :delete,
    data: { confirm: "Are you sure?" } %>

    <!-- <div class="notice">Delete success</div> -->
